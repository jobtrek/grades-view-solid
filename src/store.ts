import { createStore } from 'solid-js/store'

const [gradesStore, setStore] = createStore({
  domains: {
  },
  epsic: [
  ],
  cie: []
})

const addSemester = (domain: string) => {
  // setStore('domains')
}
